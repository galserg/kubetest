apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teamcity-server.fullname" . }}-test-connection"
  labels:
    {{- include "teamcity-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teamcity-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
